VITE_API_BASE_URL=http://127.0.0.1:8000

# Intégration Django REST réelle : laisser à false (ou absent) pour ne pas masquer les erreurs API.
# true = données mock si l’endpoint renvoie 404/405/501/503 (voir webFeaturesApi + apiError.shouldUseFallback).
VITE_ENABLE_API_FALLBACK=false
